/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/kreatoai',
    trailingSlash: true,
    images:{
        domains:['cdn-icons-png.flaticon.com']
    }
};

export default nextConfig;
